apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "heart-disease-api.fullname" . }}-config
  labels:
    {{- include "heart-disease-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
