# Development-only dependencies (NOT installed in the Docker image).
# Used for EDA, experiment tracking UI, and notebook authoring.
#
# Install with:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Visualisation (EDA notebook + confusion-matrix logging)
matplotlib==3.9.4
seaborn==0.13.2

# Experiment tracking (UI runs locally, not inside the container)
mlflow==3.1.4

# Notebook environment
jupyter==1.1.1
notebook==7.5.6
ipykernel==6.31.0

# Testing
pytest==8.3.4

# Linting
ruff==0.8.6

# Report generation (scripts/generate_report.py)
python-docx==1.1.2
